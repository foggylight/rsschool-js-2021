export enum Routes {
  main = '/',
  categories = '/category',
  statistics = '/statistics',
}

export enum AppMode {
  play = 'play',
  train = 'train',
}

export enum SideMenuState {
  opened = 'opened',
  closed = 'closed',
}
